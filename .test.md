# 🛡️ Jangijoim (LLM 기반 웹 취약점 진단 및 자동 패치 플랫폼)

Jangijoim은 대규모 언어 모델(LLM)을 활용하여 타겟 웹 애플리케이션의 보안 취약점을 스캔하고, 분석 및 자동 패치 가이드를 제공하는 모의해킹 자동화 CLI 도구입니다.

---

## 📋 사전 요구 사항 (Prerequisites)

로컬 개발 및 테스트를 위해 시스템에 다음 소프트웨어가 설치되어 있어야 합니다.
* **Docker** (버전 20.10 이상)
* **Docker Compose** (선택 사항)

---

## 🚀 로컬 테스트 실행 가이드 (Docker 환경)

이 프로젝트는 도커(Docker)를 이용해 시스템 독립적이고 격리된 환경에서 모의 공격 대상(Target App)과 메인 스캐너를 구동하여 테스트를 진행합니다. 아래 단계
를 순서대로 실행하십시오.

### 1. 도커 컨테이너 빌드
프로젝트 루트 디렉토리(`Dockerfile`이 위치한 곳)에서 다음 명령어를 실행하여 도커 이미지를 생성합니다.

```bash
docker build -t jangijoim:latest .
```

### 2. 도커 테스트 환경 접속
빌드된 이미지를 바탕으로 임시 컨테이너를 실행하고, 내부의 리눅스 bash 쉘(Shell)로 진입합니다. 
> 💡 `--rm` 옵션이 적용되어 있어, 테스트 종료 후(`exit` 입력 시) 쓰레기를 남기지 않고 컨테이너가 깔끔하게 삭제됩니다.
```bash
docker run --rm -it --entrypoint /bin/bash jangijoim:latest
```

### 3. 공격 테스트용 모의 웹 서버 동작
컨테이너 내부에서 모의 타겟 애플리케이션(`target_app_mock`) 디렉토리로 이동한 뒤, 서버를 백그라운드(`&`) 프로세스로 실행합니다.
```bash
cd target_app_mock/ && uvicorn app:app --host 127.0.0.1 --port 3000 --reload &
```

### 4. 메인 컨트롤러 실행
서버가 구동되면 루트 디렉토리로 복귀하여 메인 컨트롤러 프로세스를 백그라운드(`&`)로 실행합니다.
```bash
cd /app && python main.py serve &
```

### 5. 취약점 스캔(공격) 시작
타겟 서버와 메인 컨트롤러가 모두 구동된 상태에서, 스캔 명령어를 입력하여 로컬에 띄워진 모의 서버(3000번 포트)에 대한 공격 테스트를 시작합니다.
```bash
python main.py scan start http://127.0.0.1:3000 ./target_app_mock
```

---

## 🛠️ 주요 명령어 (CLI Usage)

컨테이너 내부 쉘에서 사용할 수 있는 `main.py`의 기본 CLI 명령어입니다.

* **도움말 확인:**
  ```bash
  python main.py --help
  ```
* **스캔 모듈 단독 실행:**
  ```bash
  python main.py scan --help
  ```

---

## 🤝 기여하기 (Contributing)
이 프로젝트의 브랜치 전략 및 코드 기여 규칙은 `CONTRIBUTING.md` 파일을 참고해 주십시오. 코드 충돌과 파이프라인 붕괴를 막기 위해 반드시 지정된 Git 워크플로우를 준수해야 합니다.